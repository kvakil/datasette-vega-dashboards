{
  "databases": {
    "sleep_log": {
      "allow_sql": {
        "id": "root"
      },
      "queries": {
        "insert_sleep": {
          "hide_sql": true,
          "sql": "INSERT INTO sleep_log (date,start,end,number_wakeups,wakeup_time,subjective_quality) VALUES (:date,:start,:end,:number_wakeups,:wakeup_time,:subjective_quality)",
          "write": true
        },
        "read_sleep": {
          "hide_sql": true,
          "sql": "SELECT * FROM sleep_log ORDER BY date DESC LIMIT 20",
          "write": false
        }
      }
    }
  }
}
